


class Observe{
    constructor(){

    }
    updateObservationStats(io,data){
        //const {username,task} = data;
        ////io.in(`observing_${task}`).emit('update-observation-stats',{username,task});
    }
}

module.exports = new Observe();